CREATE DATABASE Organization;

use Organization;

CREATE TABLE Employee(emp_no int primary key,emp_name char(20) not null,DOB date, address varchar(30), doj date, mobile_no int not null, dept_no int not null, salary int);

CREATE TABLE department (dept_no int primary key, dept_name char(20) not null, location char(20) not null);

